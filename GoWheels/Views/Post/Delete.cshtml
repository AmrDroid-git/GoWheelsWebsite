@model GoWheels.Models.Post
@{
    ViewData["Title"] = "Delete Post";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0">
                        <i class="fa-solid fa-trash me-2"></i>
                        Delete Post
                    </h4>
                </div>
                <div class="card-body text-center">
                    <!-- Vehicle Info -->
                    <h5 class="mb-3">
                        <i class="fa-solid fa-car text-primary me-2"></i>
                        @Model.Constructor @Model.ModelName
                    </h5>
                    
                    <!-- Simple Warning -->
                    <div class="alert alert-warning mb-4">
                        <i class="fa-solid fa-exclamation-circle me-2"></i>
                        The post will be marked as deleted and will no longer be visible to other users.
                    </div>

                    <!-- Action Buttons -->
                    <div class="d-grid gap-3">
                        <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-secondary">
                            <i class="fa-solid fa-times me-1"></i> Cancel
                        </a>
                        
                        <form asp-action="Delete" method="post">
                            @Html.AntiForgeryToken()
                            <input type="hidden" asp-for="Id" />
                            <button type="submit" class="btn btn-danger w-100">
                                <i class="fa-solid fa-trash me-1"></i> Mark as Deleted
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>  